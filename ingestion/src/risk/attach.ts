/**
 * Re-export from @quantara/shared — function moved there so backend can import.
 * Gap 1 — issue #115.
 *
 * Ingestion code continues to work without any import changes.
 */
export { attachRiskRecommendation } from "@quantara/shared";
